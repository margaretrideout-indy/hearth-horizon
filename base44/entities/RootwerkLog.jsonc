{
  "name": "RootwerkLog",
  "type": "object",
  "properties": {
    "user_id": {
      "type": "string",
      "description": "ID of the user who created this entry"
    },
    "user_email": {
      "type": "string",
      "description": "Email of the user"
    },
    "entry_body": {
      "type": "string",
      "description": "The journal entry text"
    },
    "sentiment_tag": {
      "type": "string",
      "enum": [
        "Rooted",
        "Reaching",
        "Weathering",
        "Blooming"
      ],
      "description": "Emotional state tag for the entry"
    },
    "is_private": {
      "type": "boolean",
      "default": true,
      "description": "Whether this entry is private or shared to the Embers feed"
    },
    "description": {
      "type": "string",
      "description": "Optional description or notes about this log entry",
      "maxLength": 1000
    }
  },
  "required": [
    "user_id",
    "entry_body",
    "sentiment_tag"
  ],
  "rls": {
    "create": {
      "data.user_id": "{{user.id}}"
    },
    "read": {
      "data.user_id": "{{user.id}}"
    },
    "update": {
      "data.user_id": "{{user.id}}"
    },
    "delete": {
      "data.user_id": "{{user.id}}"
    }
  }
}