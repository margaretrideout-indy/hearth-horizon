{
  "name": "DailyCheckIn",
  "type": "object",
  "properties": {
    "mood": {
      "type": "string",
      "enum": [
        "hopeful",
        "anxious",
        "motivated",
        "grieving",
        "confident",
        "uncertain"
      ],
      "description": "Current emotional state"
    },
    "reflection": {
      "type": "string",
      "description": "User's written reflection"
    },
    "prompt_used": {
      "type": "string",
      "description": "The prompt that was shown"
    },
    "gratitude_note": {
      "type": "string",
      "description": "Something they value about their journey"
    },
    "description": {
      "type": "string",
      "description": "Optional description or notes about this check-in",
      "maxLength": 1000
    }
  },
  "required": [
    "mood"
  ],
  "rls": {
    "create": {
      "created_by": "{{user.email}}"
    },
    "read": {
      "created_by": "{{user.email}}"
    },
    "update": {
      "created_by": "{{user.email}}"
    },
    "delete": {
      "created_by": "{{user.email}}"
    },
    "write": {
      "created_by": "{{user.email}}"
    }
  }
}